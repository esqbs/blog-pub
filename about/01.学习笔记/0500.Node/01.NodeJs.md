---
title: NodeJs
date: 2024-11-17 10:22:43
---

# NodeJs

- Node原理
	- Node bindings 实现js调用c++
		- V8 提供js运行时环境
			- 基本概念
				- isolate
					- 拥有自己堆的vm实例
				- handle
					- 对堆中的对象的引用，GC对其进行管理
					- 分为Local和persistent，类似于作用域
				- scope
					- 分为HandleScope和Context::Scope， 类似于生命周期
				- context
			- 垃圾回收
		- libuv 提供异步I/O和事件循环
			- Network I/O
				- 根据OS平台不同，分别使用Linux上的epoll，OSX和BSD类OS上的kqueue，SunOS上的event ports以及Windows上的IOCP机制
			- 其他I/O以及用户代码使用线程池
				- File I/O
				- DNS Ops
				- User Code
		- 其他c++库，如http_parser、OpenSSL、zlib等
- node标准库
	- 模块
		- module
			- 模块系统
				- cjs
					- 确认为cjs的情况
						- .cjs后缀名
						- package.json中type:"commonjs"，.js后缀名
						- `-experimental-default-type commonjs` - 默认
						- `--input-type commonjs` - 默认
						- 实用require导入的文件
					- 缓存 `Module._cache`或`require.cache`, 实用`filename`作为缓存key
					- 内置模块 `Module.builtinModules`, 实用`node:`前缀会绕过缓存
					- 循环 - *后导入的文件*会导入*先导入文件*的未完成副本
					- 导入 - `require`
						- 文件
							- 有后缀
							- 无后缀 - 依次添加`.js`、`.json`、`.node`后缀尝试加载
						- 文件夹
							- package.json中的`main`
							- 尝试`index.js`或`index.node`
						- `node_modules`文件夹
						- 全局`node_modules`文件夹
							- `Module.globalPaths`
					- 模块封装
						- `module.wrap` - `(function(exports, require, module, __filename, __dirname) { // Module code })`
						- `require`
							- `require.cache`缓存
							- `require.main == module` 是否为主入口
							- `require.resolve()`解析不加载文件，返回文件名
								- `require.resolve.paths()`
						- `module`
							- `module.require()`
							- `module.exports`
							- `module.id`
							- `module.filename`
							- `module.path`
							- `module.paths`
							- `module.childern`
							- `module.parent`
					- 自定义导入
						- `Module._extensions`
				-  esm
					- 确认为esm的情况
						- .mjs后缀名
						- package.json中type:"module", .js后缀名
						- `-experimental-default-type module`
						- `--input-type module`
						- 实用import导入的文件
					- 内置模块 - 通过`Module.syncBuiltinESMExports()`修改内置模块的属性后，用来重置
					- 导入 - `import`
						- 必须指向文件且提供文件扩展名
						- 支持的URL协议
							- `file:` - `import './foo.mjs?query=1';`
							- `node:` - `import fs from 'node:fs/promises';`
							- `data:`
								- `text/javascript`
								- `application/json`
								- `application/wasm`
						- `import()`
						- `import.meta`
							- `import.meta.dirname`
							- `import.meta.filename`
							- `import.meta.url` - `new URL('./data.proto', import.meta.url)`
							- `import.meta.resolve()`
						- json模块
						- wasm模块
					- 自定义导入
						- `Module.register()`
							- `resolve`钩子
							- `load`钩子
				- esm与cjs互操作
					- esm 导入cjs
						- `import foo from "foo";` - `module.exports = {}`
						- `import {foo} from "foo";` - `exports.foo = {}`
						- `import * as foo from "foo";` 导入所有命名导入和默认导入
				- esm和cjs区别
					- 无 `require`、`exports` 或 `module.exports`， 需要require使用`Module.createRequire()`
					- 无 `__filename` 或 `__dirname`, 使用`import.meta.dirname`和`import.meta.filename`
					- 没有插件加载，使用`Module.createRequire()`或`process.dlopen()`加载
					- 没有 require.resolve，使用`import.meta.resolve()`
					- 没有 require.extensions， 使用定制钩子
					- 没有 require.cache，ES 模块加载器有自己独立的缓存
			- 包
				- `corepack`  - 管理包管理器，类似于nvm，默认不启用
					- `corepack enable`启用
					- `corepack install --global yarn@x.y.z `安装
					- `corepack use pnpm@7.x`使用包管理器
					- package.json中的`packageManager`
				- 确定模块系统
				- 包入口
					- package.json中的`main`
					- package.json中的`exports`
						- 主入口导出 - `"exports": "./index.js"`
						- 子路径导出 - `"exports": {".": "./index.js","./submodule.js": "./src/submodule.js", "./features/*.js": "./src/features/*.js"}`
						- 条件导出 - ` "exports": {"import": "./index-module.js", "require": "./index-require.cjs"}`
							- `node-addons`
							- `node` -匹配任何 Node.js 环境
							- `import` 匹配import 或 import()
							- `require` 匹配require()
							- `module-sync`匹配import、import() 、 require()
							- `default` 默认回退
						- 匹配自定添加
							- `node --conditions=development index.js`设置条件
							- `"exports": {"development": "./dev.js"}`
				- esm和cjs双包
					- 包是cjs编写的，需要添加一个esm包装器
	- 事件
		- events
			- 观察者模式
			- `events`
				- `EventEmitter`
					- 添加监听
						- `on(eventName, listener)` - 多次调用传入相同的 eventName 和 listener 组合将导致多次添加和调用 listener
						- `once()`
						- `addListener()` - `on()`别名
						- `prependListener()`
						- `prependOnceListener()`
					- 移除监听
						- `remveListener()` - 最多从监听器数组中删除一个监听器实例
						- `removeAllListener()`
						- `off()` - `removeListener()`别名
					- 触发事件
						- `emit()`
					- 查询监听器
						- `listeners()` - 返回名为 eventName 的事件的监听器数组的副本
						- `listenerCount()` - 返回监听名为 eventName 的事件的监听器数量
						- `rawListeners()` - 返回名为 eventName 的事件的监听器数组的副本，包括任何封装器（例如由 .once() 创建的封装器）
						- `eventNames()` - 返回所有事件名
					- 最大监听数
						- `getMaxListeners()` - 返回 EventEmitter 的当前最大监听器数的值
						- `setMaxListeners(n)` - 设置最大监听数，默认10，可使用`events.defaultMaxListeners`全局设置
					- 监听异常
						- `on("error", () => {})`
					- 捕获promise的rejected
						- `events.captureRejections = true`
						- `[events.captureRejectionSymbol](err, event, ...args) {}`
				- `on()`
				- `once`
				- `getEventListeners()`
				- `getMaxListeners()`
				- `setMaxListeners()`
				- `defaultMaxListeners`
				- `captureRejections`
				- `captureRejectionSymbol`
				- `addAbortListener()`
	- 流
		 - buffer 缓冲
			 - `Buffer`
				 - 创建
					 - `Buffer.from()`
						 - 可接受`buffer`、`arrayBuffer`、`string`、`array`、`object`
						 - 接受`arrayBuffer`时与其共享内存
						 - 接受`string`时指定字符编码，`utf8`、`utf16le`、`latin1`
					- `Buffer.alloc()` - 申请一块内存，默认填充0
					- `Buffer.allocUnsafe()` - 申请一块内存，size小于`Buffer.poolSize`一半时使用内部Buffer池
					- `Buffer.allocUnsafeSlow()` - 申请一块内存
					- `Buffer.concat()` - 拼接多个Buffer实例
					- `Buffer.copyBytesFrom()` - 从`TypeArray`中复制
				- 查询
					- `buf[index]`
					- `buf.buffer`
					- `buf.byteOffset`
					- `buf.entries()`
					- `buf.keys()`
					- `buf.values()`
					- `buf.length`
					- `Buffer.isBuffer()`
					- `Buffer.isEncoding()`
					- `buffer.constants.MAX_LENGTH`
				- 操作
					- `buf[index] = 0xFF`
					- `buf.equals()`
					- `buf.compare()`
					- `buf.copy()`
					- `buf.includes()`
					- `buf.indexOf()`
					- `buf.lastIndexOf()`
					- `buf.subarray()` - 与原始内存共享相同的内存
					- `buf.swap16()`
					- `buf.swap32()`
					- `buf.swap64()`
					- `buf.toString()` - 编码支持`utf8`、`base64`、`hex`、`base64url`
					- `buf.toJSON()`
				- 读写
					- `buf.readBigInt64BE()`
					- `buf.readBigInt64LE()`
					- `buf.readBigUInt64BE()`
					- `buf.readBigUInt64LE()`
					- `buf.readInt32BE()`
					- `buf.readInt32LE()`
					- `buf.readUInt32BE()`
					- `buf.readUInt32LE()`
					- `buf.readInt16BE()`
					- `buf.readInt16LE()`
					- `buf.readUInt16BE()`
					- `buf.readUInt16LE()`
					- `buf.readIntBE()`
					- `buf.readIntLE()`
					- `buf.readUIntBE()`
					- `buf.readUIntLE()`
					- `buf.readInt8()`
					- `buf.readUInt8()`
					- `buf.readFloatBE()`
					- `buf.readFloatLE()`
					- `buf.readDoubleBE()`
					- `buf.readDoubleLE()`
					- `buf.write()`
					- `buf.writeBigInt64BE()`
					- `buf.writeBigInt64LE()`
					- `buf.writeBigUInt64BE()`
					- `buf.writeBigUInt64LE()`
					- `buf.writeInt32BE()`
					- `buf.writeInt32LE()`
					- `buf.writeUInt32BE()`
					- `buf.writeUInt32LE()`
					- `buf.writeInt16BE()`
					- `buf.writeInt16LE()`
					- `buf.writeUInt16BE()`
					- `buf.writeUInt16LE()`
					- `buf.writeIntBE()`
					- `buf.writeIntLE()`
					- `buf.writeUIntBE()`
					- `buf.writeUIntLE()`
					- `buf.writeInt8()`
					- `buf.writeUInt8()`
					- `buf.writeFloatBE()`
					- `buf.writeFloatLE()`
					- `buf.writeDoubleBE()`
					- `buf.writeDoubleLE()`
		 - stream 流
			 - 流类型
				 - 可写流
					 - `stream.writable`
						 - 事件 - `pipe`、`unpipe`、`drain`、`close`、`finish`、`error`
						 - 写流
							 - `writable.write(chunk[, encoding][, callbakc])`
							 - `writable.end([chunk[, encoding]][, callbakc])`
						 - 强制缓冲
							 - `writable.cork()` - 强制写入的数据缓存在内存
							 - `writable.uncork()` - 取消缓存
						- 销毁
							- `writable.destroy([error])` - 直接销毁流，传入error会触发error事件
						- 设置编码
							- `writable.setDefaultEncoding(encoding)`
						- 属性
							- `writable.closed`
							- `writable.destroyed`
							- `writable.writable`
							- `writable.writableAborted`
							- `writable.writableEnded`
							- `writable.writableCorked` - 完全解开流的次数
							- `writable.writableFineshed`
							- `writable.writableHighWaterMark`
							- `writable.writableLength` -
							- `writable.writableNeedDrain`
							- `writable.writableObjectMode`
							- `writable.errored`
				 - 可读流
					 - `stream.readable`
						 - 读取模式 - `readable.readableFlowing` - 值为`null`、`true`、`false`
							 - 流动
								 - `data`事件
								 - `readable.resume()`
								 - `readable.pipe()`
							 - 暂停
								 - `readable`事件
								 - `readable.pause()`
								 - `readable.unpipe()`
						 - 事件 - `pause`、`resume`、`readable`、`data`、`end`、`close`、`error`
						 - 添加到缓冲区
							 - `readable.push(chunk[, encoding])`
							 - `readable.unshift(chunk[, encoding])`
						 - 消费流
							 - `readable.read([size])`
							 - `readable.pipe(writable[, options])`
								 - `readable.unpipe([writable])`
							 - `data`事件
								 - `readable.pause()`
								 - `readable.resume()`
								 - `readable.isPaused()`
						 - 设置编码
							 - `readable.setEncoding(encoding)`
						 - 销毁
							 - `readable.destroy([error])`
						 - 迭代
							 - `readable.iterator([options])`
							 - `readable.map(fn[, options])`
							 - `readable.filter(fn[, options])`
							 - `readable.some(fn[, options])`
							 - `readable.find(fn[, options])`
							 - `readable.every(fn[, options])`
							 - `readable.flatMap(fn[, options])`
							 - `readable.reduce(fn[, initial[, options]])`
						 - 属性
							 - `readable.closed`
							 - `readable.destroyed`
							 - `readable.readable`
							 - `readable.readableAborted`
							 - `readable.readableDidRead`
							 - `readable.readableEncoding`
							 - `readable.readableEnded`
							 - `readable.readableFlowing`- 反映 Readable 流的当前状态
							 - `readable.readableHighWaterMark`
							 - `readable.readableLength`
							 - `readable.readableOjbectMode`
				 - 双工流
					 - `stream.Duplex` - 双工流是同时实现 Readable 和 Writable 接口的流
						 - `duplex.allowHalfOpen` - 如果为 false，则流将在可读端结束时自动结束可写端，默认为 true
				 - 转换流
					 - `stream.Transform` - 转换流是 Duplex 流，其中输出以某种方式与输入相关
						 - `transform.destroy([error])`
			 - `stream`
				 - `stream.duplePair([options])` - 返回包含两个双工流的数组，一端写入，另一端读取
				 - `stream.finished(stream[, options], callback)`
				 - `stream.pipeline(streams, callback)`
				 - `stream.Readable.from(iterable[, options])`
				 - `stream.Readable.fromWeb(readableStream[, options])`
				 - `stream.Readable.toWeb(readable[, options])`
				 - `stream.Writable.fromWeb(writableStream[, options])`
				 - `stream.Writable.toWeb(writable)`
				 - `stream.Duplex.from(src)`
				 - `stream.Duplex.fromWeb(pair[, options])`
				 - `stream.Duplux.toWeb(duplex)`
				 - `stream.getDefaultHighWaterMark(objectMode)`
				 - `stream.setDefaultHighWaterMark(objectMode, value)`
			 - 自定义流
				 - 可写流
					 - `new stream.Writable([options])`
						 - `highWaterMark`
						 - `decodeStrings`
						 - `defaultEncoding`
						 - `objectMode`
						 - `emitClose`
						 - `autoDestroy`
						 - `write`
						 - `writev`
						 - `destroy`
						 - `final`
						 - `construct`
						 - `signal`
					 - `writable._construct(callback)`
					 - `writable._write(chunk, encoding, callback)`
					 - `writable._writev(chunks, callback)`
					 - `writable._destroy(err, callback)`
					 - `writable._final(callback)`
				 - 可读流
					 - `new stream.Readable([options])`
						 - `highWaterMark`
						 - `encoding`
						 - `objectMode`
						 - `emitClose`
						 - `autoDestroy`
						 - `read`
						 - `destroy`
						 - `construct`
						 - `signal`
					 - `readable._construct(callback)`
					 - `readable._read(size)`
					 - `readable._destroy(err, callback)`
				 - 双工流
					 - `new stream.Duplex([options])`
						 - `allowHalfOpen`
						 - `readable`
						 - `writable`
						 - `readableObjectMode`
						 - `writableObjectMode`
						 - `readableHighWaterMark`
						 - `readableHighWaterMark`
					 - 读流或写流的接口
				 - 转换流
					 - `new stream.Transform([options])`
						 - `transform`
						 - `flush`
					 - `transform._transform(chunk, encoding, callback)`
					 - `transform._flush(callback)`
		 - stream web 网络流
			 - `ReadableStream`
			 - `WritableStream`
			 - `TramsformStream`
			 - `TextEncoderStream`
			 - `TextDecoderStream`
			 - `CompressionStream`
			 - `DecompressionStream`
		 - readline 逐行读取
			 - `readline`
				 - `readline.createInterface(options)`
					 -  事件 `line`、`history`、`pause`、`resume`、`close`、`SIGINT`、`SIGCONT`、`SIGSTP`
					 - `question(query[, options], callback)`
					 - `wirte(data[, key])`
					 - `setPrompt(prompt)`
					 - `getPrompt()`
					 - `prompt([preserveCursor])`
					 - `pause()`
					 - `resume()`
					 - `close()`
					 - `line`
					 - `cursor`
					 - `getCursorPos()`
				 - `readline.clearLine(stream, dir[, callback])`
				 - `readline.clearScreenDown(stream[, callback])`
				 - `readline.cursorTo(stream, x[, y][, callback])`
				 - `readline.moveCursor(stream, dx, dy[, callback])`
	- 线程和进程
		- worker_threads 工作线程
			- `worker_threads`
				- `worker_threads.isMainThread`
				- `worker_threads.threadId`
				- `worker_threads.SHARE_ENV` - 当前线程和工作线程共享同一组环境变量的读写访问
				- `worker_threads.parentPort`
				- `worker_threads.postMessageToThread(threadId, value[, transferList][, timeout])`
				- `worker_threads.moveMessagePortToContext(port, contextifiedSandbox)`
				- `worker_threads.recieveMessageOnPort(port)`
				- `worker_threads.getEnvironmentData(key)`
				- `worker_threads.setEnvironmentData(key[, value])`
				- `worker_threads.workerData`
				- `worker_threads.markAsUntransferable(object)`
				- `worker_threads.isMarkedAsUntransferable(object)`
				- `worker_threads.markAsUncloneable(object)`
				- `worker_threads.Worker`
					- 事件 - `message`、`messageerror`、`online`、`exit`、`error`
					- `new Worker(filename[, options])`
						- `filename`
						- `options`
							- `name`
							- `argv` - worker的`process.argv`
							- `execArgv` - worker的`process.execArgv`
							- `env`
							- `eval`
							- `stdin`
							- `stdout` - 默认`process.stdout`
							- `stderr` - 默认`process.stderr`
							- `workerData`
							- `trackUnmanagedFds `
							- `transferList`
							- `resourceLimits`
								- `maxOldGenerationSizeMb`
								- `maxYoungGenerationSizeMb`
								- `codeRangeSizeMb`
								- `stackSizeMb`
					- `worker.postMessage(value[, transferList])`
					- `worker.ref()`
					- `worker.unref()`
					- `worker.terminate()`
					- `worker.stdin`
					- `worker.stdout`
					- `worker.stderr`
					- `worker.threadId`
				- `worker_threads.MessageChannel`
					- 事件 - `message`、`messageerror`、`close`
					- `port.postMessage(value[, transferList])`
					- `port.start()`
					- `port.close()`
					- `port.ref()`
					- `port.unref()`
					- `port.hasRef()`
				- `worker_threads.BreadcastChannel`
					- `new BreadcastChannel(name)`
					- `breadcastChannel.onmessage`
					- `broadcastChannel.onmessageerror`
					- `broadcastChannel.postMessage(message)`
					- `broadcastChannel.ref()`
					- `broadcastChannel.unref()`
					- `broadcastChannel.close()`
		- child_process 子进程
			- `child_process.spawn(command[, args][, options])`
				- `options`
					 - `cwd`
					 - `env` - 默认： `process.env`
					 - `argv0` - 默认：command参数
					 - `stdio` - `pipe`、`overlapped`、`ignore`、`inherit`
					 - `detached` - 子进程独立于父进程运行
					 - `uid`
					 - `gid`
					 - `serialization` - `json`、`advanced`
					 - `shell` - `true`在shell 内运行命令， `string`为指定的shell，默认false
					 - `signal` - 中止信号
					 - `killSignal` - 默认： `SIGTERM`
					  - `timeout` - 超时时间，默认0
					 - `windowsVerbatimArguments` - 在windows上不为参数加上引号或转义
					 - `windowsHide` - 隐藏控制台窗口
				- `stdio: "ignore", detached: true`并且`unref()`可使子进程独立运行
			- `child_process.exec(command[, options][, callback])`
				- `options`
					- `cwd` - 默认： `process.cwd()`
					- `env`  - 默认： `process.env`
					- `encoding`
					- `uid`
					- `gid`
					- `shell` - 默认：Unix上为`/bin/sh`，windows上为`process.env.ComSpec`
					- `signal`
					- `killSignal`
					- `timeout`
					- `maxBuffer`
					- `windowHide`
				- `callback`
					- `error`
					- `stdout`
					- `stderr`
			- `child_process.execFile(file[, args][, options][, callback])`
				- `options`
					- `cwd`
					- `env` - 默认值：`process.env`
					- `encoding`
					- `uid`
					- `gid`
					- `shell` - 默认false
					- `signal`
					- `killSignal`
					- `timeout`
					- `maxBuffer`
					- `windowsHide`
					- `windowsVerbatimArguments`
			- `child_process.fork(modulePath[, args][, options])`
				- `options`
					- `cwd`
					- `env`- 默认值：`process.env`
					- `execPath` - 用于创建子进程的可执行文件， 即node的路径
					- `execArgv` - 可执行文件的参数列表，默认值：`process.execArgv`
					- `silent` - `true`使用管道传输到父进程，`false`使用继承，默认`false`
					- `stdio` - 会覆盖`silent`
					- `detached`
					- `uid`
					- `gid`
					- `serialization`
					- `signal`
					- `killSignal`
					- `timeout`
					- `windowVerbatimArguments`
			- `ChildProcess`
				- 事件 - `message`、`spawn`、`disconnect`、`exit`、`close`、`error`
				- `subprocess.send(message[, sendHandle[, options]][, callback])`
				- `subprocess.channel`
					- `ref()`
					- `unref()`
				- `subprocess.connected`
				- `subprocess.disconnect()`
				- `subprocess.ref()`
				- `subprocess.unref()`
				 -`subprocess.kill([signal])`
				 - `subprocess.killed`
				 - `subprocess.exitCode`
				 - `subprocess.signalCode`
				 - `subprocess.spawnargs`
				 - `subprocess.spawnfile`
				 - `subprocess.stdin`
				 - `subprocess.stdout`
				 - `subprocess.stderr`
				 - `subprocess.stdio`
		- cluster 集群
			- `cluster`
				- 事件 - `fork`、`online`、`message`、`listen`、`setup`、`disconnect`、`exit`
				- `cluster.fork([env])` - 使用child_process.fork()创建工作进程
				- `cluster.isPrimary`
				- `cluster.isWorker`
				- `cluster.worker` - 仅工作进程可用
				- `cluster.workers`  - 仅主进程可用
				- `cluster.disconnect([callback])` - 调用所有worker进程的disconnect()
				- `cluster.schedulingPolicy` - 主进程的分发策略： 1. 循环（除windows之外的平台默认）2. 由操作系统调度
				- `cluster.settngs`
					- `cwd`
					- `exec`
					- `args`
					- `execArgv`
					- `serialization`
					- `silent`
					- `stdio`
					- `uid`
					- `gid`
					- `inspectPort`
					- `windowsHide`
				- `cluster.setupPrimary([settings])`
				- `cluster.Worker`
					- 事件 - `online`、`message`、`listening`、`disconnect`、`exit`、`error`
					- `worker.send(message[, sendHandle[, options]][, callback])`
					- `worker.isConnected()`
					- `worker.exitedAfterDisconnect`
					- `worker.disconnect()` - 关闭当前进程的所有服务器，断开IPC
					- `worker.kill([signal])` - 杀死当前进程
					- `worker.isDead()`
					- `worker.id`
					- `worker.process`
		- async_hooks 异步钩子（类似线程本地存储）
		- process 进程
			- 事件
				- `worker`
				- `message`
				- `workerMessage` - postMessageToThead()
				- `disconnect`
				- `beforeExit`
				- `exit`
				- `warning` - node警告，可通过`emitWarning`触发自定义警告
				- `unhandledRejection` - 为捕获的promise的reject
				- `rejectionHandled`
				- `uncaughtExceptionMonitor`
				- `uncaughtException` - 未知异常，清理资源
				- 信号事件
			- 进程信息
				 - `process.env` - 环境变量
				 - `process.argv`-  参数
				 - `process.argv0` -  `argv[0]` 原始值的只读副本
				 - `process.execArgv` - node命令行参数
				 - `process.execPath` - node的路径
				 - `process.cwd()` - 工作目录
				 - `process.pid`
				 - `process.ppid`
				 - `process.arch`
				 - `process.platfom`
				 - `process.title`
				 - `process.version`
				 - `process.versions`
				 - `process.uptime()` - 进程运行时间
				 - `process.allowedNodeEnvironmentFlags`
				 - `process.config`
				 - `process.release`
				 - `process.debugPort`
				 - `process.stdin`
				 - `process.stdout`
				 - `process.stderr`
				 - `process.channel`
				 - `process.connected`
				 - `process.memoryUsage()`
				 - `process.constrainedMemory()`
				 - `process.availableMemory()`
				 - `process.cpuUsage([previousValue])`
				 - `process.resourceUsage()`
			- 操作
				 - `process.exitCode` - 指定退出时的状态码
				 - `process.exit([code])` - code可覆盖`exitCode`
				 - `process.kill(pid[, signal])` - 杀死指定进程
				 - `process.aborb()`
				 - `process.disconnect()`
				 - `process.send(message[, sendHandle[, options]][, callback])`
				 - `process.dlopen(module, filename[, flags])`
				 - `process.nextTick(callback[, ...args])`
				 - `process.chdir(directory)` - 修改当前进程的工作目录
				 - `process.loadEnvFile(path)`
				 - `process.hrtime.bigint()` - 高精度时间
				 - `process.getBuiltinModule(id)`
			- 其他
				- `process.emitWarning(warning[, options])`
				- `process.hasUncaughtExceptionCaptureCallback()`
				- `process.setUncaughtExceptionCaptureCallback()`
				- `process.sourceMapEnabled`
				- `process.setSourceMapEnabled(val)`
				- `process.noDeprecation`
				- `process.traceDeprecation`
				- `process.throwDeprecation`
				- `process.finalization`
					- `register(ref,  callback)`
					- `registerBeforeExit(ref, callback)`
					- `unregister(ref)`
				- `process.report`
					- `writeReport([filename][, err])`
					- `getReport([err])`
					- `directory`
					- `filename`
					- `compact`
					- `reportOnFatalError`
					- `reportOnSignal`
					- `reportOnUncaughtException`
					- `signal`
				- `process.permission`
					- `has(scope[,rederence])`
				- `process.features`
					- `cached_builtins`
					- `debug`
					- `inspector`
					- `ipv6`
					- `require_module`
					- `tls`
					- `tls_alpn`
					- `tls_ocsp`
					- `tls_sni`
					- `typescript`
					- `uv`
	- I/O
		 - 网络
			 - dns 域名服务器
				 - `dns.lookup(hostname[, options], callback)`  - 类似于`ping`命令解析
					 - `options`
						 - `family` - 值4、6、0，默认0
						 - `hints` - `dns.ADDRCONFIG` 、`dns.V4MAPPED`、`dns.ALL`
						 - `all` - true 返回所有解析的地址，默认false
						 - `order` - `verbatim`、`ipv4first`、`ipv6first`
				 - `dns.lookupService(address, port, callback)`
				 - `dns.resolve(hostname[, rrtype], callback)` - 使用网络DNS服务解析，可设置dns 服务器
					- `rrtype`
						  - `A` - IPv4地址
						  - `AAAA` - IPv6地址
						  - `ANY` - 任何记录
						  - `CAA` - CA授权记录
						  - `CNAME` - 规范名称记录
						  - `MX` - 邮件交换记录
						  - `NAPRT` - 名称权限指针记录
						  - `NS` - 名称服务器记录
						  - `PTR`-  指针记录
						  - `SOA` - 权限记录的开始
						  - `SRV` - 服务记录
						  - `TXT` - 文本记录
				- `dns.setServers(servers)`
				- `dns.getServers(servers)`
				- `dns.reverse(ip, callback)`
			 - dgram UDP/数据报套接字
			 - net TCP
			 - http
			 - http2
			 - https
			 - tls 安全传输层
		 - 文件
			 - fs 文件
				- 文件夹
					 - `fs.mkdir(path[, options][, callback])`
						 - `options`
							 - `recursive` - 级联 默认false
							 - `mode`
					 - `fs.mkdtemp(prefix[, options], callback)`
						 - `optinos`
							 - `encoding`
					 - `fs.copy(src, dest[, options][, callback])`
						 - `options`
							 - `dereference` - 取消符号连接 默认false
							 - `errorOnExist` - force为false且目标存在时，抛出错误
							 - `filter`
							 - `force`
							 - `mode`
							 - `perserveTimestamps` - 保留src的时间戳
							 - `recursive`
							 - `verbatimSymlinks` - 为true，跳过符号连接
					 - `fs.rmdir(path[, options][, callback])`
						 - `options`
							 - `recursive`
							 - `maxRetries` - 重试次数
							 - `retryDelay` - 失败重试的时间间隔
					 - `fs.opendir(path[, options], callback)`
						 - `options`
							 - `encoding`
							 - `bufferSize`
							 - `recursive`
						 - `callback`
							 - `fs.Dir`
								 - `dir.close(callback)`
								 - `dir.path`
								 - `dir.read(callback)`
									- `callback`
										- `fs.Dirent`
											- `dirent.isBlockDevice()`
											- `dirent.isCharacterDevice()`
											- `dirent.isDirectory()`
											- `dirent.isFIFO()`
											- `dirent.isFile()`
											- `dirent.isSocket()`
											- `dirent.isSymbolicLink()`
											- `dirent.name`
											- `dirent.path`
					 - `fs.readdir(path[, options], callback)`
						- `options`
							- `encoding`
							- `withFileTypes`
							- `recursive`
				- 文件
					- `fs.open(path[, flags[, mode]], callback)`
						- `fs.write(fd, buffer[, options], callback)`
							- `options`
								- `offset`
								- `length`
								- `position`
						- `fs.write(fd, string[, position[, encoding]], callback)`
						- `fs.writev(fd, buffers[, position], callback)`
						- `fs.read(fd, buffer[, options], callback)`
							- `options`
								- `offset`
								- `length`
								- `position`
						- `fs.readv(fd, buffers[, postion], callback)`
						- `fs.fsync(fd, callback)` - 将数据刷新到存储设备
						- `fs.fdatasync(fd, callback)` - 同`fsync`类似，但不会flush修改的`metadata`
						- `fs.close(fd[, callback])`
						- `fs.fchmod(fd, mode, callback)`
						- `fs.fchown(fd, uid, gid, callback)`
						- `fs.fstat(fd[, options], callback)`
						- `fs.futimes(fd, atime, mtime, callback)`
						- `fs.ftruncate(fd[, len], callback)`
					- `fs.copyFile(src, dest[, mode], callback)`
					- `fs.writeFile(file, data[, options], callback)`
						- `options`
							- `encoding`
							- `mode`
							- `flag`
							- `flush`- 使用`fs.fsync()` 来刷新数据
							- `signal`
					- `fs.appendFile(path, data[, options], callback)`
						- `options`
							- `encoding`
							- `mode`
							- `flag`
							- `flush`
					- `fs.openAsBlob(path[, options])`
					- `fs.readFile(path[, options], callback)`
						- `options`
							- `encoding`
							- `flag`
							- `signal`
					- `fs.truncate(path[, len], callback)` - 截断文件；len：默认0，清空
					- `fs.glob(pattern[, options], callback)`
					- `fs.constants`
					- 文件流
						- `fs.createReadStream(path[, options])`
						- `fs.createWriteStream(path[, options])`
					- 监听
						 - `fs.watch(filename[, options][, listener])`
							 - `optins`
								 - `persistent` - 监视文件时进程不退出
								 - `recursive`
								 - `encoding`
								 - `signal` - 中止信号
						 - `fs.watchFile(filename[, options], listener)`
							 - `options`
								 - `bigint`
								 - `persistent`
								 - `interval` - 轮询频率
						 - `fs.unwatchFile(filename[, listener])`
				- 操作（文件或文件夹）
					- `fs.rm(path[, options], callback)`
						- `options`
							- `force`
							- `recursive`
							- `maxRetries`
							- `retryDelay`
					- `fs.rename(oldPath, newPath, callback)` - 重命名或移动
					- `fs.realPath(path[, options], callback)` - 解析规范路径
					- `fs.stat(path[, options], callback)`
						- `stats.isBlockDevice()`
						- `stats.isCharacterDevice()`
						- `stats.isDirectory()`
						- `stats.isFIFO()`
						- `stats.isFile()`
						- `stats.isSocket()`
						- `stats.isSymbolicLink()`
						- `stats.dev` - 文件设备的数字标识
						- `stats.ino` - 文件系统索引节点编号
						- `stats.mode`
						- `stats.nlink` - 硬链接数
						- `stats.uid`
						- `stats.gid`
						- `stats.rdev`
						- `stats.size` - 文件大小
						- `stats.blksize` - 块大小
						- `stats.blocks` - 块数
						- `stats.mtimeMs`
						- `stats.ctimeMs`
						- `stats.birthtimeMs`
						- `stats.atimeNs`
						- `stats.mtimeNs`
						- `stats.ctimeNs`
						- `stats.birthtimeNs`
						- `stats.atime` - 访问文件的时间戳
						- `stats.mtime` - 修改文件的时间戳
						- `stats.ctime` - 更改文件状态的时间戳
						- `stats.birthtime` - 创建时间戳
					- 权限
						- `fs.access(path[, mode])` - 访问权限
						- `fs.chmod(path, mode)`
						- `fs.chown(path, uid, gid)`
					- 链接
						- `fs.link(existingPath, newPath, callback)` - 和原文件共享相同存储，类似别名
						- `fs.symlink(target, path[, type], callback)` - 存储指向原文件的路径，类似指针
							 - `fs.lchmod(path, mode, callback)`
							 - `fs.lchown(path, uid, gid)`
							 - `fs.lstat(path[, options], callback)`
							 - `fs.lutimes(path, atime, mtime, callback)`
							 - `fs.readlink(path[, options], callback)` - 读取 path 引用的符号链接的内容
						- `fs.unlink(path, callback)`
	- 工具
		 - crypto 加密
		 - webcrypto 网络加密
		 - zlib 压缩
		 - os 操作系统
			 - 系统信息
				 - `os.hostname()` - 主机名
				 - `os.arch()`
				 - `os.platform()`
				 - `os.machine()`
				 - `os.type()`
				 - `os.version()`
				 - `os.release()`
				 - `os.cpus()`
				 - `os.freemem()`
				 - `os.totalmem()`
				 - `os.loadavg()`
				 - `os.uptime()` - 操作系统运行时间
				 - `os.userInfo([options])`
				 - `os.homedir()` - 用户目录
				 - `os.tmpdir()` - 临时目录
				 - `os.EOL` - 行尾标记 window: `\r\n`，posix: `\n`
				 - `os.devNull` - devnull 路径
				 - `os.networkInterfaces()` - 网络接口信息
				 - `os.constants`
					 - `signals` - 信号量
					 - `errno` - 错误
					 - `dlopen`
					 - `priority`
			 - 操作
				 - `os.getPriority([pid])`
				 - `os.setPriority([pid, ]priority)` - 参数priority范围： -20到19
		 - path 路径
		 - querystring 查询字符串
		 - string_decoder 字符串解码
		 - timer 定时器
		 - tty 终端
			 - `tty.ReadStream`
				 - `readStream.isRaw`
				 - `readStream.isTTY`
				 - `readStream.setRawMode(mode)`
			 - `tty.WriteStream`
				 - 事件 - `resize`
				 - `writeStream.rows`
				 - `writeStream.columns`
				 - `writeStream.isTTY`
				 - `writeStream.clearLine(dir[, callback])`
				 - `writeStream.clearScreenDown([callbakc])`
				 - `writeStream.cursorTo(x[, y][, callback])`
				 - `writeStream.moveCursor(dx, dy[, callback])`
				 - `writeStream.getColorDepth([env])`
				 - `writeStream.hasColors([count][, env])`
				 - `writeStream.getWindowSize()`
		 - url 网址
		 - util 实用工具
		 - v8 引擎
		 - vm 虚拟机
	- 调试和监控
		 - error 错误
		 - assert 断言
		 - test 测试
		 - console 控制台
		 - debugger 调试器
		 - inspector 检查器
		 - domain 域 监听域范围内的事件或error事件
		 - perf_hooks 性能钩子
	- 链接库
		 - c++ 插件
		 - wasl  WebAssembly 系统接口
	- node相关
		 - CLI命令行
		 - corepack 管理包管理器 与package.json 中packageManager关联
		 - 国际化
		 - 权限
		 - repl 交互式解释器
		 - report 诊断报告
		 - sea 单个可执行应用程序
		 - trace_events 跟踪事件
