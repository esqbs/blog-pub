---
title: ubuntu安装docker
date: 2024-06-14 21:10:08
permalink: /pages/cde336/
categories:
  - 学习笔记
  - Docker
tags: []
---

# ubuntu安装docker

## 安装ssh

```bash
$ sudo apt install openssh-server

$ sudo systemctl status ssh # 查看ssh状态

$ sudo ufw allow ssh # 防火墙允许ssh
```

## 查看系统版本

```bash
$ cat /proc/version

或

$ lsb_release -d # 下面安装了lsb-release
```

## 更换apt源

```bash
$ sudo cp /etc/apt/sources.list /etc/apt/sources.list.backup

$ sudo vi /etc/apt/sources.list

// ubuntu 22版本 阿里源
deb http://mirrors.aliyun.com/ubuntu/ jammy main restricted universe multiverse
deb-src http://mirrors.aliyun.com/ubuntu/ jammy main restricted universe multiverse
deb http://mirrors.aliyun.com/ubuntu/ jammy-security main restricted universe multiverse
deb-src http://mirrors.aliyun.com/ubuntu/ jammy-security main restricted universe multiverse
deb http://mirrors.aliyun.com/ubuntu/ jammy-updates main restricted universe multiverse
deb-src http://mirrors.aliyun.com/ubuntu/ jammy-updates main restricted universe multiverse
deb http://mirrors.aliyun.com/ubuntu/ jammy-proposed main restricted universe multiverse
deb-src http://mirrors.aliyun.com/ubuntu/ jammy-proposed main restricted universe multiverse
deb http://mirrors.aliyun.com/ubuntu/ jammy-backports main restricted universe multiverse
deb-src http://mirrors.aliyun.com/ubuntu/ jammy-backports main restricted universe multiverse


$ sudo apt update
```

## 安装常用包

```bash
$ sudo apt install net-tools # 网

$ sudo apt install apt-transport-https curl lsb-release # apt使用https curl linux标准库
```

## 安装docker

```bash
$ curl -fsSL https://mirrors.aliyun.com/docker-ce/linux/ubuntu/gpg | sudo gpg --dearmor -o /usr/share/keyrings/docker-archive-keyring.gpg # 为了确认所下载软件包的合法性，需要添加软件源的 GPG 密钥

$ echo \
  "deb [arch=amd64 signed-by=/usr/share/keyrings/docker-archive-keyring.gpg] https://mirrors.aliyun.com/docker-ce/linux/ubuntu \
  $(lsb_release -cs) stable" | sudo tee /etc/apt/sources.list.d/docker.list > /dev/null
  # 需要向 sources.list 中添加 Docker 软件源
  
$ sudo apt update
$ sudo apt-get install docker-ce docker-ce-cli containerd.io docker-compose-plugin


$ sudo docker run hello-world # 测试一下

$ sudo gpasswd -a $USER docker #将当前用户加入到Docker用户组
$ newgrp docker #更新用户组
```

## docker 命令

```bash
$ docker run -itd ubuntu /bin/bash # 启动容器
# -i: 交互式操作
# -t: 终端，exit退出
# -d: 后台运行
# -p: <主机端口>:<容器端口> 端口映射
# ubuntu: ubuntu 镜像
# /bin/bash：命令，会替换Dockerfile中的CMD

$ docker container ls # 查看启动的容器
# -a 查看所有容器
# -q 只查看容器id，一般用于命令嵌套

$ docker container stop <容器id> # 停止容器
$ docker container start <容器id> # 运行容器
$ docker container restart <容器id> # 重启容器
$ docker exec -it <容器id> /bin/bash # 进入容器
$ docker container rm <容器id> #删除容器
$ docker ps # 查看正在运行的容器


$ docker search <镜像名> # 查找镜像
$ docker pull <镜像名> # 获取镜像
$ docker images # 列出本地主机上的镜像
$ docker image rm <镜像名> # 删除镜像
$ docker build -t <镜像名> . # 构建镜像， 最后有个点

$ docker network create -d bridge <网络名> #新建网络
# -d：参数指定 Docker 网络类型，有 bridge、overlay
# $ docker run -itd --name u1 --network <网络名> ubuntu /bin/bash
# $ docker run -itd --name u2 --network <网络名> ubuntu /bin/bash
# 两个容器连接到同一网络，就可以通过名称访问，名称代替容器主机IP

```

## Dockerfile

| Dockerfile 指令 | 说明                                                         |
| :-------------- | :----------------------------------------------------------- |
| FROM            | 指定基础镜像，用于后续的指令构建                             |
| LABEL           | 添加镜像的元数据，使用键值对的形式                           |
| RUN             | 在构建过程中在镜像中执行命令                                 |
| CMD             | 指定容器创建时的默认命令<br/>可以被docker run中的命令覆盖<br/>如果存在多个 CMD 指令，仅最后一个生效 |
| ENTRYPOINT      | 设置容器创建时的主要命令<br/>不可被docker run中的命令覆盖<br/>存在多个 ENTRYPOINT 指令，仅最后一个生效 |
| EXPOSE          | 声明容器运行时监听的特定网络端口                             |
| ENV             | 在容器内部设置环境变量                                       |
| ADD             | 将文件、目录或远程URL复制到镜像中，推荐直接用COPY            |
| COPY            | 将文件或目录复制到镜像中                                     |
| VOLUME          | 为容器创建挂载点或声明卷                                     |
| WORKDIR         | 设置后续指令的工作目录                                       |
| USER            | 指定后续指令的用户上下文                                     |
| ARG             | 定义在构建过程中传递给构建器的变量，可使用 "docker build" 命令设置 |
| ONBUILD         | 当该镜像被用作另一个构建过程的基础时，添加触发器             |
| STOPSIGNAL      | 设置发送给容器以退出的系统调用信号                           |
| HEALTHCHECK     | 定义周期性检查容器健康状态的命令                             |
| SHELL           | 覆盖Docker中默认的shell，用于RUN、CMD和ENTRYPOINT指令        |

## docker-compose

[命令说明 · Docker -- 从入门到实践 (docker-practice.github.io)](https://docker-practice.github.io/zh-cn/compose/commands.html)

[Compose 模板文件 · Docker -- 从入门到实践 (docker-practice.github.io)](https://docker-practice.github.io/zh-cn/compose/compose_file.html)

## docker资源网站

[前言 · Docker -- 从入门到实践 (docker-practice.github.io)](https://docker-practice.github.io/zh-cn/)

[Dockerfile reference | Docker Docs](https://docs.docker.com/reference/dockerfile/)

[https://hub.docker.com](https://hub.docker.com/)
